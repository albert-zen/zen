# Long-Term Optimization 001 Evidence

## Codex Worker Note

Round: 1
Issue: long-term-optimization-001: Secure capability-based local transport
Local tracker state transition: In Progress -> Agent Review
Branch: codex/long-term-optimization-001
PR URL: not configured; local-branch mode
Base revision/diff scope: 05477f7b54829a10d9fd79e18ca9e5247f1bd418 through codex/long-term-optimization-001; issue 001 transport, config, CLI, browser transport, proxy, corresponding tests, evidence, tracker, and the manager-approved dogfood capability caller only
Summary of behavior delivered: Added unguessable capability authentication for HTTP requests and SSE, authenticated the Node client, removed CORS/preflight support, enforced explicit non-loopback opt-in, added one-time standalone CLI handoff, and made Vite a trusted same-origin capability-injecting proxy without exposing credentials to browser code.
Final scope summary: Completed all issue 001 acceptance criteria without changing AppServer dispatch/lifecycle, ThreadManager, runtime/tool approval, persistence, UI projection/components, dependencies, PRD, or DAG. Manager-approved scope expansion was limited to src/dogfood-acceptance.ts and its transport capability call.
Changed files/modules: src/app-server-transport.ts; src/app-server-config.ts; src/app-server-cli.ts; src/web-dev-cli.ts; src/web-ui-client.ts; src/dogfood-acceptance.ts; web/vite.config.ts; test/app-server-transport.test.ts; test/app-server-config.test.ts; test/web-ui-client.test.ts; test/web-dev-proxy.test.ts; docs/implementation/long-term-optimization-001-evidence.md; docs/implementation/long-term-optimization-tracker.md
Tests added/updated: Public request and SSE authorization; authenticated Node request/SSE ordering; generated capability entropy; CORS/preflight removal; non-loopback opt-in; protocol token redaction; one-time handoff consumption; browser same-origin routes without credentials; real Vite config request/SSE proxy smoke; existing transport, Web UI, and dogfood callers updated for required Node capability.
Acceptance criteria status: PASS - missing and mismatched request/SSE capabilities return 401 before app data; PASS - correct capability supports requests and ordered SSE; PASS - wildcard CORS and OPTIONS path removed; PASS - loopback default and explicit remote opt-in enforced; PASS - generated capabilities use 256 random bits, provided capabilities are validated, protocol errors/routine logs do not include tokens, and standalone CLI supports exclusive one-time handoff; PASS - browser uses relative same-origin routes and real Vite config injects capability server-side for request/SSE.
Commands run and results: Baseline scoped tests: 4 files/16 tests passed. RED/GREEN request gate: RED HTTP 200 instead of 401, GREEN 1 passed. RED/GREEN Node client: RED HTTP 401, GREEN 1 passed. RED/GREEN SSE gate: RED HTTP 200 instead of 401, GREEN 1 passed. RED/GREEN authenticated SSE: RED notification timeout, GREEN ordered stream test passed. RED/GREEN CORS removal: RED wildcard allow-origin present, GREEN no CORS headers. RED/GREEN bind policy: RED non-loopback bind succeeded, GREEN explicit opt-in test passed. RED/GREEN redaction: RED token appeared in 500 body, GREEN generic body passed. RED/GREEN handoff: RED missing function, GREEN one-time consume passed. RED/GREEN proxy: RED missing proxy helper, GREEN real Vite config request/SSE smoke passed. RED/GREEN remote config: RED missing parser, GREEN explicit parser passed. RED/GREEN browser path: RED cross-origin absolute request, GREEN relative same-origin request/SSE passed. Final targeted: npm test -- test/app-server-transport.test.ts test/app-server-config.test.ts test/web-ui-client.test.ts test/web-dev-proxy.test.ts test/dogfood-acceptance.test.ts - 5 files/25 tests passed. npm run typecheck - passed. npm run typecheck:web - passed. npm test - 28 files/160 tests passed. npm run build - passed. npm run web:build - passed. git diff --check - passed. Optional standalone subprocess probe was rejected by shell policy before execution; no process/artifact was created, and the same handoff path is covered by the real Vite config smoke test.
Validation log paths: none; command outcomes are recorded above
Required check status or local-check handoff reason: All required local checks pass; GitHub checks/PR are not configured because this program uses local-branch mode.
Evidence links/paths: docs/implementation/long-term-optimization-001-evidence.md
Decisions made: Authorization uses a Bearer capability compared through fixed-size SHA-256 digests with timingSafeEqual; generated tokens contain 256 random bits; provided tokens require at least 32 bytes and no whitespace/control characters; standalone handoff is an exclusive mode-0600 JSON file atomically claimed and deleted by the Node consumer; manager approved the narrow src/dogfood-acceptance.ts caller expansion after the initial scope blocker.
Standards notes: Tracer-bullet tests exercise public HTTP, SSE, Node-client, browser-client, filesystem handoff, and real Vite proxy behavior. Capability protocol knowledge remains concentrated in app-server-transport; bind/handoff configuration remains in app-server-config. Item-first runtime and AppServer behavior were not changed.
Reviewer notes: Review from exact base 05477f7b54829a10d9fd79e18ca9e5247f1bd418. Confirm security behavior on both endpoints, no CORS headers/OPTIONS special case, no browser credential path, CLI handoff cleanup, and scoped dogfood caller change. Do not use a worker-owned reviewer; manager will assign fresh context.
Open questions: none
Known residual risks: Filesystem permission enforcement is platform-dependent; the handoff is additionally exclusive, atomically claimed, and deleted after consumption. Browser workspace still passes its existing baseUrl option, but BrowserAppServerTransportClient deliberately ignores it and uses same-origin relative routes; changing the out-of-scope workspace controls was not approved.
Blocker or context escalation details: Initial scope blocker was resolved by manager approval limited to src/dogfood-acceptance.ts and directly corresponding expectations solely to pass transport.capability into HttpAppServerClient; no further scope expansion occurred.

## Codex Review Note

Round: 1
Issue: long-term-optimization-001: Secure capability-based local transport
Reviewer context: fresh
Reviewer edits: none
Reviewed branch: codex/long-term-optimization-001 at 26669abda3adc52365c7b4472fac386b92c57f8f
Base revision/diff scope: 05477f7b54829a10d9fd79e18ca9e5247f1bd418..26669abda3adc52365c7b4472fac386b92c57f8f
Standards Review blocking: 1) Vite trusted proxy must disable permissive CORS and reject foreign Origin or cross-site Fetch Metadata before capability injection; unauthorized or foreign-origin request, preflight, and SSE must be rejected before AppServer dispatch. 2) Replace caller-selected handoff files with caller-selected directories and unique owned artifacts that are fully written and flushed before publication, include a random ownership marker, are cleaned up only when the current artifact still matches that marker, and are atomically claimed and deleted. 3) Provided capability mode and generated one-time handoff mode must be mutually exclusive; reject configuration that supplies both. web-dev-cli must use only its in-process generated capability and trusted proxy.
Standards Review non-blocking: 4) Remove the stale direct browser baseUrl interface and server URL query/localStorage/control from web/src/workspace.tsx. 5) Add public capability validation tests for short, whitespace, and control-character provided values. 6) Add captured-output tests proving CLI/proxy routine logs never contain the capability. 7) Add and run a real subprocess acceptance test against the built standalone CLI proving unique handoff publication once, claim once, authenticated request and SSE, redacted output, and replacement-artifact survival at shutdown. 8) Add proxy tests for successful same-origin plus unauthorized/foreign-origin request, preflight, and SSE with no dispatch or side effects. 9) Keep targeted/full tests, core/Web typechecks, Node/Web builds, and diff check green.
Standards Review missing evidence: Built standalone CLI subprocess acceptance and captured CLI/proxy output redaction evidence were missing; the proxy smoke did not prove rejection before dispatch.
Spec Review blocking: The trusted proxy remained a capability-confused deputy for foreign-origin browser traffic; handoff publication/cleanup did not establish unique ownership or replacement safety; external credential and handoff modes were not mutually exclusive.
Spec Review non-blocking: Retire docs/implementation/alb-93-real-web-ui-smoke.mjs and record retirement because issue 007 will provide canonical Playwright coverage. Update README.md with secure standalone handoff, trusted Web proxy, and remote opt-in behavior.
Spec Review missing evidence: No public tests covered invalid provided capability forms, proxy rejection paths with dispatch-side-effect checks, routine-log redaction, or the built standalone CLI lifecycle.
Local tracker state decision: Rework
State decision reason: Blocking security and ownership findings are clear and bounded. Manager approved scope expansion only for web/src/workspace.tsx direct-server removal, README.md secure workflow documentation, docs/implementation/alb-93-real-web-ui-smoke.mjs removal, and a new CLI subprocess test/harness with corresponding expectations. Do not touch ThreadManager/AppServer lifecycle, approval, persistence, or issue 002 source.

## Codex Worker Note

Round: 2
Issue: long-term-optimization-001: Secure capability-based local transport
Local tracker state transition: In Progress -> Agent Review
Branch: codex/long-term-optimization-001
PR URL: not configured; local-branch mode
Base revision/diff scope: 05477f7b54829a10d9fd79e18ca9e5247f1bd418 through codex/long-term-optimization-001; issue 001 transport/config/CLI/client/proxy sources, corresponding tests, approved browser workspace and dogfood callers, README, harness retirement, evidence, and tracker only
Summary of behavior delivered: Closed the Round 1 confused-deputy, handoff-ownership, and mode-selection findings. The trusted Vite proxy now injects the capability only after same-origin trust checks, rejects preflight/foreign/cross-site/unattributed request and SSE traffic before dispatch, and emits no permissive CORS headers. Standalone generated handoff now publishes one fully written and flushed unique artifact in a selected directory, includes random ownership, supports atomic one-time claim/delete, and preserves replacement artifacts during cleanup. Provided and generated modes are mutually exclusive. Browser direct-server configuration was removed, output redaction and built-CLI lifecycle were exercised, README was updated, and the obsolete smoke harness was retired for issue 007 replacement.
Final scope summary: Completed every Round 1 blocking, non-blocking, and missing-evidence item within manager-approved scope. Manager architecture decisions were followed: provided capability and generated handoff are exclusive; web-dev-cli uses only an in-process generated capability; standalone callers select a handoff directory while claimants receive the published artifact path; browser code has no capability or server URL; README/workspace/harness/subprocess expansions were limited to those explicitly approved. The earlier manager-approved src/dogfood-acceptance.ts change remains solely to pass the capability into HttpAppServerClient. No ThreadManager/AppServer lifecycle, approval/tool runtime, persistence, issue 002 source, dependency, PRD, or DAG changes were made.
Changed files/modules: README.md; docs/implementation/alb-93-real-web-ui-smoke.mjs (retired); docs/implementation/long-term-optimization-001-evidence.md; docs/implementation/long-term-optimization-tracker.md; src/app-server-cli.ts; src/app-server-config.ts; src/app-server-transport.ts; src/dogfood-acceptance.ts; src/web-dev-cli.ts; src/web-ui-client.ts; test/app-server-cli.test.ts; test/app-server-config.test.ts; test/app-server-transport.test.ts; test/web-dev-proxy.test.ts; test/web-static.test.ts; test/web-ui-client.test.ts; web/src/workspace.tsx; web/vite.config.ts
Tests added/updated: Public transport capability rejection/authenticated request/SSE tests; short/whitespace/control-character validation; unique complete handoff publication, atomic claim-once, ownership cleanup, and replacement survival; exclusive credential modes; built standalone CLI subprocess conflict/redaction/publication/claim/authenticated request/SSE/shutdown lifecycle; real Vite same-origin request/SSE success, captured-log redaction, external-mode conflict, and unauthorized/foreign/cross-site/preflight/request/SSE rejection with zero AppServer dispatch/subscription effects; browser same-origin-only interface and static removal of direct-server UI/config; approved dogfood capability caller expectation.
Acceptance criteria status: PASS - missing/mismatching request and SSE capabilities return 401 with no app data or dispatch. PASS - matching capability supports Node requests and ordered SSE notifications. PASS - App Server and Vite emit no wildcard CORS path, OPTIONS is not authorized, direct cross-origin browser configuration is removed, and proxy preflight/foreign/cross-site/unattributed traffic is rejected before injection/dispatch. PASS - loopback remains default and App Server/Web non-loopback binds require explicit opt-in. PASS - generated/provided values are absent from protocol bodies and captured routine CLI/proxy output; generated values have 256 random bits; provided values reject short, whitespace, or control-character forms; standalone CLI publishes a unique flushed one-time owned handoff and never deletes a replacement. PASS - Web uses relative same-origin request/SSE routes; web-dev-cli keeps the capability in-process and standalone Vite atomically claims or receives it server-side; browser code receives neither capability nor target URL.
Commands run and results: Round 2 proxy RED: foreign request reached AppServer and returned 500 instead of 403; unattributed request also reached AppServer and returned 500. Proxy GREEN: same-origin request/SSE passed and unauthorized/foreign/cross-site/preflight/request/SSE cases returned 403 with zero dispatch/subscription side effects. Handoff/mode RED: public tests failed on missing directory publisher, ownership cleanup, and exact-one-mode parser; GREEN after unique flushed publication, ownership marker, claim/delete, replacement-preserving cleanup, and mode enforcement. Browser RED: static public test found DEFAULT_SERVER_URL/direct server UI; GREEN after interface/query/localStorage/control removal. Built CLI RED: subprocess completed publish/claim/request/SSE but timed out because graceful IPC shutdown was absent; GREEN after bounded shutdown support, with one handoff, one claim, authenticated request/SSE, redacted output, exit 0, and replacement preserved. Invalid provided-value and captured-log regressions entered GREEN against the hardened validator/redacted output. Initial targeted command (7 files/34 tests) passed. A test-only ForkOptions type mismatch was found by npm run typecheck, removed, and the CLI regression (1 file/2 tests) plus typecheck passed. Strengthened ownership/CLI regression (2 files/8 tests) passed. Final npm run typecheck passed; npm run typecheck:web passed; npm test passed 29 files/167 tests; npm run build passed; npm run web:build passed; git diff --check passed.
Validation log paths: none; RED/GREEN and final command outcomes are recorded above
Required check status or local-check handoff reason: All required local checks pass on the final working revision. GitHub checks and PR are unavailable because the program remains in local-branch mode.
Evidence links/paths: docs/implementation/long-term-optimization-001-evidence.md
Decisions made: Proxy trust requires a matching same-origin Origin or affirmative same-origin Fetch Metadata, rejects conflicting metadata and every preflight, and remains layered over capability authentication rather than replacing it. Generated handoff publication links a fully written and fsynced private temporary inode to a random final path, then removes the temporary name. Claim and deletion use atomic rename; shutdown checks filesystem identity and the random ownership marker before cleanup. Standalone Vite receives either a provided capability or a one-time artifact path, while web-dev-cli bypasses external credential modes and wires its generated capability directly. A small parent-process shutdown message exists only to exercise graceful built-CLI cleanup deterministically on Windows. The obsolete alb-93 executable harness is retired; issue 007 owns canonical Playwright browser coverage.
Standards notes: Tracer-bullet fixes were driven through public HTTP/SSE, Vite, filesystem, browser-interface, and real subprocess boundaries. Capability protocol behavior stays in app-server-transport; credential/bind/handoff policy stays in app-server-config; no new dependency or cross-domain abstraction was introduced. README documents secure standalone handoff, trusted Web proxy, and explicit remote opt-in.
Reviewer notes: Review the full diff from exact base 05477f7b54829a10d9fd79e18ca9e5247f1bd418. Focus on proxy rejection before capability injection/dispatch, marker-owned atomic handoff lifecycle and replacement safety, exact-one external mode behavior, browser removal, captured redaction, and the built CLI subprocess evidence. The manager will assign a fresh reviewer; no worker-owned reviewer was spawned.
Open questions: none
Known residual risks: Mode-0600 enforcement follows host filesystem semantics. Same-origin proxy checks depend on browser Origin/Fetch Metadata behavior, but missing metadata is rejected and the App Server capability remains independently mandatory, so this is not Origin-only protection. Canonical browser automation remains deferred to issue 007 as directed.
Blocker or context escalation details: none. Round 1 manager-approved expansions were limited to web/src/workspace.tsx, README.md, retiring docs/implementation/alb-93-real-web-ui-smoke.mjs, and the new CLI subprocess test/harness; the prior manager-approved dogfood caller expansion remains unchanged. No further expansion was required.
